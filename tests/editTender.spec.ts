import { test, expect } from '../fixtures/fixture';

test.describe('Edit tender functionality check', () => {
    test.beforeEach(async () => {
        
    })
    test("C237 - Edit the pending tender with valid values", async () => {

    })
})